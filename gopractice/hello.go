import "strings"
import "fmt"

func finder (string test) {
	string login = "0000"
	string message = "0001"
	string command = "0002"

	if(string.Contains(test,message)) {
		fmt.Println("message")
	}
}

package main
